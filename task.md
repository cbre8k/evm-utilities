# Refactoring Progress

- `[x]` Styling Foundations
  - `[x]` Modify `_variables.scss` (typography, sharp corners, instant transitions)
  - `[x]` Modify `global.scss` (light and dark high-contrast themes, Ant Design overrides)
- `[x]` Reusable UI Components
  - `[x]` Create `TerminalWindow.tsx`
  - `[x]` Create `BinaryDeco.tsx`
  - `[x]` Create `AsciiDivider.tsx`
  - `[x]` Create `GlitchText.tsx`
  - `[x]` Create `ScanlineOverlay.tsx`
  - `[x]` Export new components and update primitive components in `src/components/ui/`
- `[x]` Layout Integration
  - `[x]` Update `AppLayout.tsx` (brutalist bars, logo treatment, system status footer)
  - `[x]` Update `Layout.module.scss` (sharp borders, instant snapping hover states)
- `[x]` Page Integration
  - `[x]` Update `src/app/page.tsx` (simulator console wrapper, retro copy)
  - `[x]` Update `src/app/explorer/ExplorerClient.tsx` (tables, loading/empty states)
- `[x]` Build and Verify
  - `[x]` Run build to verify compilation
  - `[x]` Run linter to verify code cleanliness and styling guidelines

- `[x]` `/goal` Follow-ups
  - `[x]` Hide window controls `[-][+][x]` in `TerminalWindow` if no callback methods are provided
  - `[x]` Fix header in `AppLayout.tsx` to remove duplicate stats (e.g. remove hardcoded STATUS: ONLINE since we show dynamic stats in footer)
  - `[x]` Enable vertical scrolling for the forms inside `page.tsx` simulator workspace (fixed via `TerminalWindow` wrapper constraints)
  - `[x]` Implement barcode animations/decorations for status, stages, and loading elements
  - `[x]` Implement sensitive RPC URL input masking (scrambling all characters including structural `://` and `.`) and block click selection/copy
  - `[x]` Fix terminal output height constraints so only the inner canvas content scrolls
  - `[x]` Remove inner border/header in `Terminal.tsx` to avoid double border (2px) and double title bar visual glitches
  - `[x]` Collapse adjacent borders (using `margin-top: -1px` in `.workspace` and `.resultLayout`) to ensure clean 1px borders everywhere
  - `[x]` Hide the AI Agent FAB button (`AI AGENT`) when closed, while preserving drawer toggle logic
  - `[x]` Prevent users from viewing default/system RPC URLs when focusing/clicking the sensitive input field (replaces the string with `[ SYSTEM RPC - SECURED ]` and highlights it for replacement)
  - `[x]` Expand bit-scrambling output to 200 characters to ensure it covers the full width of the input field container
  - `[x]` Apply sensitive input masking and bit-scrambling animation to the explorer page RPC URL field in `ExplorerInputBar.tsx`
  - `[x]` Fix simulator status to blink correctly when executing/canceling (`styles.statusBlink` and `styles.statusWarning` added)
  - `[x]` Remove duplicate dynamic status texts and barcode scanners from explorer loading empty state panel (`ExplorerEmptyState.tsx`)
  - `[x]` Display the Sourcify signature database connection status (`ONLINE`, `OFFLINE`, `LOADING`) dynamically in the Signature Lookup top stats bar on the `misc` page
  - `[x]` Create a slot-machine-style rolling character selector (`SlotStatus` component) for simulator status transitions (777 roll animation)
  - `[x]` Pad all status names to exactly 9 characters (`READY`, `EXECUTING`, `FAILED`, `SUCCESS`, `CANCELING`, `CANCELLED`) to prevent visual shifting
  - `[x]` Remove the barcode scanner component from the simulator status text container (keeping it only for pipeline stages and loading states)

- `[x]` Explorer Workspace Tab Formatting
  - `[x]` Replace space with underscore in `[ FUND FLOW ]` and `[ GAS PROFILER ]` in `ExplorerClient.tsx`
  - `[x]` Reduce `fontSize` prop from `12` to `10` in `ExplorerResultWorkspace.tsx` TabBar
  - `[x]` Reduce `fontSize` prop from `12` to `10` in `ExplorerTraceLoadingWorkspace.tsx` TabBar
  - `[x]` Remove `TX_OVERVIEW` and `TRACE_WORKSPACE` TerminalWindow headers from the Explorer page
  - `[x]` Completely remove `ExplorerStatusBar` to clean up duplicate/redundant status messages during loading or error states
  - `[x]` Ensure result columns (transaction rail and trace workspace) have a clean border cover
  - `[x]` Align the transaction header rail title box (`.txHeaderTitle`) and workspace tab bar (`.toolbar`) heights exactly (`42px`) to ensure perfect horizontal divider alignment across panels
  - `[x]` Refactor CSS badges (for event count, state storage size, etc.) into outline terminal tag "hack style"
  - `[x]` Unified the trace summary loading workspace (`ExplorerTraceLoadingWorkspace.tsx`) to use the same animated ASCII retro-spinner as the main exploration load screen
  - `[x]` Unified the network badge on the simulator page to use the shared `<Badge>` component styled as an outline hacker tag with matching `fontSize={9}`
  - `[x]` Clickable `@jim` Author Link: Unified the `@jim` author link on the Explorer page by importing constants (`AUTHOR`, `GITHUB`) and adding pointer cursor + click handler, matching Simulator and Misc pages.
  - `[x]` Unified `@jim` font size to exactly `16px` across all three pages (Simulator, Explorer, and Misc).
  - `[x]` Ensure all page layouts have border cover: Added a solid `1px solid var(--border-default)` border to the root `.page` container on all 3 pages (Simulator, Explorer, and Misc), wrapping their entire content blocks (including banners, input headers, and main panels) in a clean brutalist frame.
  - `[x]` Unified header/stats banner padding to exactly `16px` (`$space-4`) on all three pages (Simulator, Explorer, and Misc).
  - `[x]` Verify changes and test compilation
